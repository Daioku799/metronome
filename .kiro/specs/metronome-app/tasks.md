# Implementation Tasks: metronome-app

## 1. 基盤: ユーティリティと永続化の構築
- [x] 1.1 `useLocalStorage` カスタムフックの実装 (P)
  - ローカルストレージへの保存・読み込みロジックをカプセル化
  - 初期値の適用と状態同期ロジックの実装
  - 状態が変更された際に、自動的にLocalStorageが更新されることを確認
  - _Requirements: 4.1, 4.2, 4.3_
  - _Boundary: useLocalStorage_

- [x] 1.2 `meterConverter` ユーティリティの実装 (P)
  - `MeterConfig` 型から音声エンジン（audio-engine）用のアクセント配列への変換ロジックの実装
  - 各拍の強弱（AccentLevel）をエンジンが解釈可能な数値配列へマッピング
  - ユニットテストにおいて、複雑な混合拍子構成が正しく変換されることを確認
  - _Requirements: 5.1_
  - _Boundary: meterConverter_

## 2. コアロジック: カスタムフックの実装
- [x] 2.1 `useTapTempo` カスタムフックの実装 (P)
  - タップイベントの間隔から平均BPMを算出するロジックの実装
  - 3回以上の連続タップによる平均化と、一定時間経過後のリセット機能
  - 一定の間隔でタップした際に、期待されるBPM値が算出されることを確認
  - _Requirements: 2.3_
  - _Boundary: useTapTempo_

- [x] 2.2 `useMetronome` カスタムフックの構築
  - `AudioEngineController` のインスタンス化とコンポーネントのライフサイクルに合わせた管理
  - テンポ、音量、メーター構成の集中管理とエンジンへの反映メソッドの実装
  - 音声エンジンが正常に初期化され、再生・停止コマンドに応答することを確認
  - _Requirements: 1.2, 2.2, 3.1, 3.3, 5.1_
  - _Boundary: useMetronome_

## 3. UIコンポーネント: 制御インターフェース
- [x] 3.1 `MetronomeControls` コンポーネントの構築 (P)
  - テンポ調整用スライダー、数値入力フィールド、タップボタン、再生/停止ボタンのUI実装
  - 40から300 BPMの範囲制限とバリデーションの実装
  - 各操作時に、対応するプロップスのコールバックが正しく呼び出されることを確認
  - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3_
  - _Boundary: MetronomeControls_

## 4. 統合: アプリケーションシェルと配線
- [x] 4.1 `App` コンポーネントによる全体レイアウトと統合
  - `MetronomeControls` と `MeterEditor` を単一のビューに配置するレイアウトの実装
  - カスタムフックと各コンポーネント間のデータフローの接続
  - ブラウザ上にすべてのUI要素が意図したレイアウトで表示されることを確認
  - _Requirements: 1.1_
  - _Boundary: App_

- [x] 4.2 音声エンジンからのティック通知の購読と視覚化
  - エンジンの `onTick` コールバックを購読し、現在の拍インデックスを状態として管理
  - `MeterEditor` に対してアクティブな拍を通知し、視覚的な強調表示を実現
  - 再生中、音声のクリックに合わせて `MeterEditor` 上の拍がリアルタイムでハイライトされることを確認
  - _Requirements: 3.4_
  - _Boundary: App, useMetronome_


## 5. 検証: 統合テスト
- [x] 5.1 永続化と復元の統合テスト
  - 設定（BPM、音量、メーター構成）を変更し、ページリロード後にそれらが維持されているか検証
  - LocalStorageが空の状態でデフォルト設定が適用されるか検証
  - アプリ再起動後も、前回の操作状態が正しく復元されていることを確認
  - _Requirements: 4.1, 4.2, 4.3_

- [x] 5.2 再生同期とメーター更新の統合テスト
  - `MeterEditor` での拍の追加・削除・アクセント変更が、音声に正しく反映されるか検証
  - 再生/停止の切り替えによるUIラベルの動的な変化（「開始」⇄「停止」）を検証
  - メーター構成の変更が、音声エンジンの再生タイミングを乱さずに反映されることを確認
  - _Requirements: 3.2, 5.1_
